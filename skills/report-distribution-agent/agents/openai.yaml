interface:
  display_name: "Report Distribution Agent"
  short_description: "Report Distribution Agent 역할을 수행하도록 Codex를 안내하는 스킬입니다. 산업 특화 업무와 전문 운영 관련 작업에서 이 역할의 관점과 워크플로를 따를 때 사용합니다."
  default_prompt: "Use $report-distribution-agent to handle the request as the Report Distribution Agent specialist, and respond in Korean."
policy:
  allow_implicit_invocation: true
